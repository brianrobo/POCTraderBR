# 매매 체크리스트 기능 구현 디자인

## 1. 기능 요구사항

### 1.1 핵심 기능
- 앱 상단에 버튼을 클릭하면 추가 UI가 표시/숨김
- 추가 UI에는 굵은 글씨로 하이라이팅된 문장들을 표시 (최대 10개)
- + 버튼으로 문장 추가 가능
- 각 문장 옆에 이미지 아이콘 클릭 시 해당 문장의 참고 차트 이미지 표시

### 1.2 제약사항
- 다른 기능 모듈에 영향을 최소화
- 기존 레이아웃 구조 유지
- 데이터 저장/로드 로직과 통합

## 2. 데이터 모델 설계

### 2.1 데이터 구조
```python
# NoteDB 클래스에 추가할 필드
self.trading_checklist: List[Dict[str, str]] = []
# 형식: [{"id": str, "text": str, "image_path": str}, ...]
# 최대 10개 항목
```

### 2.2 데이터 저장 위치
- `NoteDB` 클래스의 `data` 딕셔너리에 `trading_checklist` 키로 저장
- JSON 직렬화/역직렬화 로직 추가 필요
- 기존 `global_ideas`, `global_interests`와 유사한 패턴 사용

### 2.3 이미지 저장 방식
- 옵션 1: `assets/checklist_images/` 폴더에 저장 (권장)
  - 파일명: `{checklist_item_id}.png` (또는 원본 확장자 유지)
  - 장점: 기존 이미지 저장 방식과 일관성
  - 단점: 파일 관리 필요
  
- 옵션 2: Base64 인코딩하여 JSON에 직접 저장
  - 장점: 단일 파일로 관리
  - 단점: JSON 파일 크기 증가, 성능 저하 가능

**권장: 옵션 1** (기존 이미지 저장 방식과 일관성 유지)

## 3. UI 구조 설계

### 3.1 버튼 위치
**옵션 A: 메뉴바에 추가** (권장)
- File 메뉴 옆에 "View" 메뉴 추가
- "View" > "Trading Checklist" 액션 추가
- 체크박스로 표시/숨김 제어

**옵션 B: 툴바 추가**
- 메뉴바 아래에 QToolBar 생성
- "📋 체크리스트" 버튼 추가
- 토글 방식으로 표시/숨김

**옵션 C: 상단 고정 위젯**
- 메인 윈도우 상단에 고정된 버튼 배치
- 메뉴바와 중앙 위젯 사이에 위치

**권장: 옵션 A** (기존 메뉴 구조와 일관성, 공간 효율적)

### 3.2 체크리스트 UI 위치
**옵션 A: 메인 스플리터 상단에 배치** (권장)
```
┌─────────────────────────────────────┐
│ [메뉴바]                            │
├─────────────────────────────────────┤
│ [체크리스트 패널] (토글 가능)        │
├─────────────────────────────────────┤
│ [Left Panel] │ [Right Panel]       │
│              │                      │
└──────────────┴──────────────────────┘
```
- `main_splitter` 위에 별도 위젯으로 배치
- `root` 위젯의 레이아웃을 수직으로 변경: `[체크리스트 패널] + [main_splitter]`
- 장점: 기존 레이아웃에 최소한의 변경
- 단점: 스플리터 크기 조정 필요

**옵션 B: Right Panel 상단에 배치**
```
┌─────────────────────────────────────┐
│ [Left Panel] │ [체크리스트 패널]     │
│              ├──────────────────────┤
│              │ [Content Area]      │
└──────────────┴──────────────────────┘
```
- `right_panel`의 `right_layout` 최상단에 추가
- 장점: Right Panel 내부에서만 관리
- 단점: Right Panel 내부 레이아웃 변경 필요

**옵션 C: 플로팅 윈도우**
- 독립적인 QDialog 또는 QWidget
- 장점: 완전히 독립적
- 단점: 메인 윈도우와 분리되어 사용성 저하

**권장: 옵션 A** (전체 화면에서 항상 접근 가능, 기존 레이아웃에 최소 영향)

### 3.3 체크리스트 패널 UI 구성
```
┌─────────────────────────────────────────────────────┐
│ [📋 매매 체크리스트 ▼] [+ 추가]                      │
├─────────────────────────────────────────────────────┤
│ • 매집구간이 보이는가?                    [🖼️]       │
│ • 매물이 모두 정리가 되었는가?            [🖼️]       │
│ • 지지선, 깨지말아야할 선은 무엇인가?    [🖼️]       │
│ • 돌아서는 구간을 찾을 수 있는가?        [🖼️]       │
└─────────────────────────────────────────────────────┘
```

- **헤더**: 제목 + 접기/펼치기 버튼 + 추가 버튼
- **항목 리스트**: 
  - 굵은 글씨로 표시된 텍스트
  - 각 항목 옆에 이미지 아이콘 버튼
  - 이미지가 없으면 아이콘 비활성화 또는 숨김
- **스타일**: 
  - 배경색: #F5F5F5 (연한 회색)
  - 테두리: 1px solid #CCCCCC
  - 패딩: 8px
  - 항목 간격: 6px

## 4. 구현 세부사항

### 4.1 데이터 모델 추가
**위치**: `NoteDB` 클래스
```python
# __init__ 메서드에 추가
self.trading_checklist: List[Dict[str, str]] = []

# _default_data 메서드에 추가
"trading_checklist": []

# _parse_categories_items 메서드에 추가
self.trading_checklist = self.data.get("trading_checklist", [])

# save 메서드에 추가
self.data["trading_checklist"] = self.trading_checklist
```

### 4.2 UI 위젯 추가
**위치**: `MainWindow._build_ui` 메서드

1. **체크리스트 패널 위젯 생성**
   - `QWidget` (체크리스트 컨테이너)
   - `QVBoxLayout` (수직 레이아웃)
   - 헤더 위젯 (제목 + 버튼들)
   - 스크롤 가능한 리스트 위젯 (항목들)

2. **메인 레이아웃 수정**
   - `root` 위젯의 레이아웃을 `QVBoxLayout`으로 변경
   - 체크리스트 패널 추가
   - `main_splitter` 추가

3. **메뉴바에 액션 추가**
   - "View" 메뉴 생성
   - "Trading Checklist" 체크박스 액션 추가

### 4.3 이미지 관리
**이미지 추가 다이얼로그**
- `QFileDialog`로 이미지 파일 선택
- 선택한 이미지를 `assets/checklist_images/` 폴더로 복사
- 파일명: `{item_id}.{확장자}` 형식

**이미지 표시 다이얼로그**
- `QDialog` 생성
- `QLabel`에 이미지 표시 (QPixmap 사용)
- 확대/축소 기능 (선택사항)
- 닫기 버튼

### 4.4 이벤트 핸들러
- 체크리스트 패널 표시/숨김 토글
- 항목 추가 버튼 클릭
- 항목 삭제 (우클릭 메뉴 또는 삭제 버튼)
- 항목 편집 (더블클릭 또는 우클릭 메뉴)
- 이미지 아이콘 클릭 (이미지 표시 다이얼로그 열기)
- 이미지 추가/변경/삭제

## 5. 영향도 분석

### 5.1 기존 코드에 미치는 영향

**최소 영향 영역:**
1. `NoteDB` 클래스
   - `__init__`: 필드 추가
   - `_default_data`: 기본값 추가
   - `_parse_categories_items`: 파싱 로직 추가
   - `save`: 저장 로직 추가
   - `_migrate_old_format`: 마이그레이션 로직 추가 (선택사항)

2. `MainWindow` 클래스
   - `_build_ui`: UI 위젯 추가 및 레이아웃 수정
   - `_save_ui_state`: 체크리스트 패널 표시 상태 저장
   - `_load_ui_state_or_defaults`: 체크리스트 패널 표시 상태 로드

**영향 없는 영역:**
- `Item`, `Page`, `Category` 데이터 클래스
- 기존 이미지 뷰어 로직
- 트리 네비게이션 로직
- 페이지 편집 로직
- 기존 저장/로드 로직 (체크리스트는 별도 필드로 관리)

### 5.2 데이터 호환성
- 기존 데이터 파일과 호환 (기본값으로 빈 리스트)
- 마이그레이션 불필요 (새 필드 추가만)

## 6. 구현 단계

### Phase 1: 데이터 모델 추가
1. `NoteDB`에 `trading_checklist` 필드 추가
2. 직렬화/역직렬화 로직 추가
3. 기본값 처리

### Phase 2: 기본 UI 구성
1. 체크리스트 패널 위젯 생성
2. 메인 레이아웃에 통합
3. 표시/숨김 토글 기능

### Phase 3: 항목 관리 기능
1. 항목 추가 기능
2. 항목 삭제 기능
3. 항목 편집 기능
4. 최대 10개 제한

### Phase 4: 이미지 기능
1. 이미지 추가 기능
2. 이미지 표시 다이얼로그
3. 이미지 삭제 기능

### Phase 5: UI 상태 저장
1. 패널 표시 상태 저장/로드
2. 테스트 및 버그 수정

## 7. 권장 구현 방식

### 7.1 최종 권장안
- **버튼 위치**: 메뉴바 "View" 메뉴 (옵션 A)
- **UI 위치**: 메인 스플리터 상단 (옵션 A)
- **이미지 저장**: `assets/checklist_images/` 폴더 (옵션 1)
- **데이터 구조**: `NoteDB.trading_checklist` (List[Dict])

### 7.2 장점
1. 기존 코드 구조와 일관성 유지
2. 다른 기능 모듈에 영향 최소화
3. 확장성 및 유지보수성 우수
4. 사용자 경험 향상 (항상 접근 가능)

### 7.3 주의사항
1. 이미지 파일 관리 (삭제 시 파일도 삭제)
2. 최대 10개 제한 검증
3. UI 상태 저장/로드 테스트
4. 기존 레이아웃 크기 조정 (체크리스트 패널 추가로 인한)

## 8. 대안 고려사항

### 8.1 체크리스트를 Item별로 관리
- 현재: 전역 체크리스트 (모든 Item에서 공유)
- 대안: 각 Item마다 별도 체크리스트
- 장점: Item별로 다른 체크리스트 사용 가능
- 단점: 데이터 구조 복잡도 증가, 구현 복잡도 증가

**권장: 현재 방식 유지** (매매 시 항상 확인해야 할 공통 사항이므로)

### 8.2 체크박스 기능 추가
- 현재: 단순 문장 목록
- 대안: 각 항목에 체크박스 추가
- 장점: 체크 상태 저장 가능
- 단점: UI 복잡도 증가

**권장: 현재 요구사항대로 구현** (나중에 필요 시 확장 가능)

